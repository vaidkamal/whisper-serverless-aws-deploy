# Change this tag to trigger a new deployment through the pipeline.
# Commit + push after changing — CodePipeline picks it up automatically.
# Kamal Vaid
# Available tags:
#   v1-base  → faster-whisper base  (~74MB,  faster,  less accurate)
#   v2-small → faster-whisper small (~244MB, slower,  more accurate)

MODEL_TAG=v1-base
